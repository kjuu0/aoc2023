use std::io;

use day1::{calibration_sum_pt1, calibration_sum_pt2_regex};

fn main() {
    // println!("{}", calibration_sum_pt1(io::stdin().lock()));
    println!("{}", calibration_sum_pt2_regex(io::stdin().lock()));
}
