use std::io;

use day1::{calibration_sum_pt1, calibration_sum_pt2};

fn main() {
    // println!("{}", calibration_sum_pt1(io::stdin().lock()));
    println!("{}", calibration_sum_pt2(io::stdin().lock()));
}
